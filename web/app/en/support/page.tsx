import { englishMetadata } from "../../../lib/seo";
import { SiteHeader, SiteFooter } from "../../components/site-shell";
export const metadata = englishMetadata("support", "Google Drive Cleaner Safety Guide & Support", "Learn how DUPESPACE detects exact duplicates, protects keepers, handles restore and speeds up repeat Google Drive scans without skipping safety checks.");
export default function Page() {
  return <main><SiteHeader locale="en" /><article className="legal shell"><h1>Review confidently. Clean deliberately.</h1><h2>What counts as a duplicate?</h2><p>Files must have matching sizes and content checksums. Matching names alone are not enough. Mirrored folders must also match relative paths and verified contents; empty folders, shortcuts and unverifiable items are excluded.</p><h2>Which copy is kept?</h2><p>Protection rules have priority. Among eligible candidates, earlier creation time wins. If a group’s timestamps cannot all be compared, shorter paths provide a consistent fallback; equal timestamps also use shorter paths. Creation dates are metadata, not proof of origin. Independent projects can require identical copies.</p><h2>Trash, undo and permanent deletion</h2><p>Trash is the default. After confirmed success, Undo restores affected items without forcing a full Drive scan. Restored copies are not automatically selected again. If fresh verification is unavailable, scan again before cleaning that item. You can also use Google Drive Trash. Permanent deletion has no undo and requires explicit high-risk confirmation.</p><h2>Faster repeat scans</h2><p>An encrypted index in this browser can synchronize file changes instead of reading the whole listing again. It expires after seven days and is bound to one Google account. Folder or permission changes may require a full rebuild. File count matters more than total gigabytes for a metadata scan.</p><h2>Before large operations</h2><p>Keep an independent backup, inspect full paths and start with a small test. A failed operation never automatically switches to permanent deletion. Keep your CSV audit report privately: it can contain personal filenames and paths.</p><h2>Report a problem safely</h2><p>Use <a href="https://github.com/kaimeow1114-netizen/dupespace-duplicate-file-cleaner/issues">GitHub Issues</a> with your app version, steps and a redacted error message. Never post passwords, OAuth credentials, access tokens, private filenames or unredacted CSV reports.</p></article><SiteFooter locale="en" /></main>;
}
