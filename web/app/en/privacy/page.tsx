import { englishMetadata } from "../../../lib/seo";
import { SiteFooter, SiteHeader } from "../../components/site-shell";

export const metadata = englishMetadata("privacy", "Privacy Policy", "How DUPESPACE performs local browser and Windows file analysis, creates CSV reports and separates AdSense from the private analyzer workspace.");

export default function Page() {
  return <main lang="en"><SiteHeader pagePath="privacy" locale="en" /><article className="legal shell"><h1>Privacy policy</h1><p>Updated September 3, 2026.</p><p className="lead">DUPESPACE is local-first: file analysis stays on your device, and filenames, paths and contents do not become server or advertising data.</p><h2>Browser folder analysis</h2><p>The web analyzer can read only files you explicitly provide through the browser file picker or a drag-and-drop action. Filenames, relative paths, file contents, content fingerprints and results are processed in browser memory and are not uploaded to DUPESPACE. The web app has no permission to move, overwrite or delete local files.</p><h2>Previews and reports</h2><p>Visible image groups use temporary device-local Blob URLs for one representative preview. They are released when results change or the page closes. CSV reports are generated and downloaded inside your browser and may contain private paths and fingerprints. Do not publish unredacted reports.</p><h2>Analyzer network boundary</h2><p>The <code>/local</code> and <code>/en/local/</code> routes use a stricter Content Security Policy. They do not load AdSense, third-party analytics, external frames or social trackers.</p><h2>Windows application</h2><p>The desktop app contains no behavior telemetry. Scanning, content comparison, previews and reports run on your computer. Update checks read public GitHub Release information and verify the installer size and SHA-256 before asking you to install it.</p><h2>Legacy session migration</h2><p>The hosted cloud-cleaning feature has been retired. Its old entry attempts to revoke existing OAuth tokens and clears encrypted cookies after successful revocation. If revocation fails, the encrypted cookie is retained only for another revocation attempt until its original expiry; it cannot restore file access. Devices that never revisit cannot be remotely cleared. Old scan, preview, trash and deletion APIs no longer accept file operations. You may also revoke access manually in the original account’s third-party access settings.</p><h2>Advertising</h2><p>Public marketing, download and guide pages use Google AdSense Auto Ads. Google and its partners may use cookies under their policies. Advertising code does not load in the local analyzer and cannot access file lists, fingerprints, previews or CSV reports.</p><h2>Contact</h2><p>Report issues through <a href="https://github.com/kaimeow1114-netizen/dupespace-duplicate-file-cleaner/issues">GitHub Issues</a>. Include a version, reproduction steps and redacted errors; never post passwords, tokens, private filenames or full reports.</p></article><SiteFooter locale="en" /></main>;
}
