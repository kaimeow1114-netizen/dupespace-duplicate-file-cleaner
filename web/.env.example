# No OAuth client or Web Secret is needed for local file analysis.
# Existing deployments retain their original SESSION_SECRET only to revoke legacy sessions.
SESSION_SECRET=replace-with-at-least-32-random-characters
